{
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky",
    "lint:backend": "npm --prefix backend run lint",
    "clean:backend": "npm --prefix backend run clean"
  }
}
